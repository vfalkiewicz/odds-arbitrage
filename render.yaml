services:
  - type: web
    name: odds-arbitrage
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python main.py
    envVars:
      - key: ODDS_API_KEY
        sync: false
      - key: PORT
        value: 8000
